# Return Value, Method name, arg1, arg2, ...

# public boolean broadcast_message(String message);
boolean,broadcast_message,String

# public boolean register(String username);
boolean,register,String
